# 爺ぴーてぃー（GGPT）設計書 v0.1
GGPT — Generative Grandpa Pattern Talker

## 1. 目的
「最小構成・軽量なWebアプリ」で、短時間の雑談において“人間っぽく見える”会話体験を成立させる。
- モデルの賢さ（知識量・推論能力）ではなく、会話の自然さ（間・相槌・ゆらぎ・誤り・人格の一貫性）を重視する。
- ログイン不要・サーバ不要（静的ホスティング可）で動作する。

## 2. ゴール / 非ゴール
### ゴール
- モバイルフレンドリーなLINE風チャットUI
- ローカル保存（localStorage）に会話ログと学習状態を保持
- 古いログは自動忘却（容量/件数上限）
- ユーザーの名前・個人情報は保存しない（記憶しない）
- キャラ設定「爺（90歳）」の会話らしさをテンプレ＋確率で再現
- 単語ビグラム（カウント方式）で会話から学習し、発話を生成
- ときどき誤タイプ（自然なゆらぎ）
- 返答までの“間”をランダムに付与

### 非ゴール
- 正確な事実回答、計算、専門知識の提供
- 長期一貫記憶（個人名や過去の詳細を覚える）
- 高度な文法性・意味理解
- サーバサイド学習・クラウド同期

---

## 3. UX方針（人間っぽさの設計）
### 3.1 会話らしさの柱
- **遅延（間）**：0.6–2.5秒程度のランダム。たまに3–4秒。
- **相槌と受容**：否定を避け、感情に寄り添う短文を優先。
- **曖昧さ**：「たぶん」「よう分からんが」「昔はなあ」等。
- **弱点の正当化**：最新知識に弱い・忘れる・話が逸れる。
- **短く終える**：1〜2文、長くても3文。たまに途中で切る。
- **軽い繰り返し**：低確率で同じ言い回しを再利用。

### 3.2 NG（違和感が増える）
- 丁寧すぎるビジネス文体
- 知識マウント
- ユーザーの個人情報を当てにいく振る舞い
- 長文の説教

---

## 4. システム概要
### 4.1 実装形態
- フロントエンド単体（SPAでもMPAでも可）
- 静的配布（GitHub Pages等）で動作
- データは localStorage に保存

### 4.2 モジュール
1) UI（Chat）
- メッセージ表示（左右バブル）
- 入力欄（固定下部）
- 送信ボタン
- クリア/リセット（任意）

2) Memory（保存・忘却）
- chatLog / model / state を保存
- 上限超過時に先頭から削除

3) Tokenize（単語っぽい分割）
- 最小のルールベース分割（日本語向け）
- v0.1は「スペース区切り優先」＋「簡易句読点分離」
  - 例："さゆは うまい。" -> ["さゆは","うまい","。"]
  - 将来拡張：tiny segmenter（辞書なし）

4) Model（単語ビグラム：カウント方式）
- 遷移カウント：count[w1][w2] += 1
- 生成：直前語から次語を確率サンプリング

5) Persona（爺テンプレ・誤タイプ・遅延）
- 起動メッセージ
- 相槌テンプレ群
- 聞き返しテンプレ群
- 昔話テンプレ群
- 誤タイプ注入

---

## 5. データ設計（localStorage）
### 5.1 キー
- `ggpt.chat` : 会話ログ
- `ggpt.model` : ビグラムカウントと語彙
- `ggpt.state` : ランタイム状態（直近文脈、設定）
- `ggpt.meta` : バージョン、最終更新、上限設定

### 5.2 形式（例）
#### ggpt.chat
```json
{
  "messages": [
    {"id":"...","role":"user","text":"...","ts":173803...},
    {"id":"...","role":"ai","text":"...","ts":173803...}
  ]
}
```

#### ggpt.model
```json
{
  "vocab": {"<BOS>":1,"<EOS>":1,"。":1,"ええと":1,...},
  "bigram": {
    "ええと": {"のう": 3, "…": 1},
    "昔は": {"なあ": 4}
  }
}
```

#### ggpt.state
```json
{
  "lastTokens": ["<BOS>","ええと"],
  "seed": 12345,
  "settings": {
    "maxMessages": 300,
    "maxChars": 60000,
    "typoRate": 0.06,
    "fallbackRate": 0.35
  }
}
```

※JSON例は概念。実装は簡略可（vocabはSetでもよい）。

---

## 6. 忘却（ログ削除）
### 6.1 方針
- 会話ログが増えすぎたら**古いものから削除**。
- 忘却は「人間らしさ」でもあるため、明示的に強い制御を入れる。

### 6.2 ルール（v0.1推奨）
- `maxMessages`（例：300）を超えたら先頭から削除
- 併用で `maxChars`（例：60,000）も超えたら削除

### 6.3 モデル側の忘却
- v0.1では **モデル（bigramカウント）は保持**、ログだけ忘却
- オプション：`decay`（減衰）を入れる
  - 一定間隔で全カウントに0.995を掛ける等（軽量）
  - 新しい会話に“性格”が寄る

---

## 7. 学習（単語ビグラム）
### 7.1 学習対象
- ユーザー発話（role=user）のみ学習（基本）
- ただし爺の口調を固定するため、初期コーパス（seed）を事前投入

### 7.2 学習手順
1) 入力テキストをトークン列に変換
2) `tokens = [<BOS>] + tokens + [<EOS>]`
3) 各 i について `bigram[tokens[i]][tokens[i+1]] += 1`
4) vocabに存在しないトークンは追加（v0.1は追加を許可）

---

## 8. 生成（返答生成）
### 8.1 基本戦略（ハイブリッド）
返答は以下の混合で作る。
1) **テンプレ応答**（相槌・受容・聞き返し）
2) **ビグラム生成**（短文生成）
3) **フォールバック**（「よう分からんがのう」等）

混合比は設定で調整（例：template 55% / bigram 35% / fallback 10%）。

### 8.2 生成長
- 1〜2文（最大3文）
- 1文あたり 6〜18トークン程度で打ち切り
- 低確率で文が途中で終わる（爺っぽさ）

### 8.3 文脈
- v0.1では直近ユーザー発話からキーワードを1〜2個抽出して、テンプレに差し込む（簡易）
  - 抽出は「長めのトークン優先」「句読点除外」程度でOK

---

## 9. 誤タイプ（人間味ノイズ）
### 9.1 ルール（軽量）
- 返答テキストの一部に低確率で変換をかける
- 例：
  - かな1文字を近いかなに置換（さ→ざ、て→で 等）
  - 1文字欠落
  - 「…」を増やす

### 9.2 注意
- 破壊的になりすぎない（読める範囲）
- 連続誤タイプは避ける（1回の返答に最大1箇所）

---

## 10. UI（モバイル・LINE風）
### 10.1 画面
- ヘッダー：アプリ名「爺ぴーてぃー」
- サブ：GGPT — Generative Grandpa Pattern Talker
- メッセージ：左=爺、右=ユーザー
- 入力欄：固定フッター

### 10.2 体験
- 送信後、入力欄は即クリア
- 爺の返答は「入力中…」インジケータ→遅延→表示
- スクロールは常に最新へ

### 10.3 操作
- 「会話を消す」（chatのみ削除）
- 「すべてリセット」（chat+model+state削除）
  - デフォルトは誤操作防止で「会話を消す」だけ目立たせる

---

## 11. 初期コーパス（seed）
起動時に、モデルに以下の“爺口調”を事前投入する。
- 相槌：そうかのう／なるほどのう／そうかそうか
- 曖昧：たぶん／気がするが／よう分からんが
- 聞き返し：それはなんじゃ／もう一度言ってくれるかの
- 昔話：昔はなあ／わしの若いころは
- 自己説明：今年で90になった（たぶん）

起動メッセージ（固定）：
> はじめまして……ですかね？ 爺ぴーてぃーです。今年で90歳になりました。たぶん。

---

## 12. 計測（研究用）
最小のログとして、画面に表示しない形で以下を保持（任意）。
- 返答がテンプレ由来かビグラム由来か
- 誤タイプ発生有無
- 平均遅延

※個人情報は保存しない。ユーザー名推定や記憶は禁止。

---

## 13. マイルストーン
### v0.1（まず動く）
- UI完成
- localStorage保存
- 忘却（maxMessages/maxChars）
- seed投入
- テンプレ応答 + フォールバック

### v0.2（学習してる感）
- 単語ビグラム学習（user発話のみ）
- ビグラム生成を混合
- 返答遅延、入力中表示

### v0.3（人間味を上げる）
- 誤タイプ注入
- 直近キーワード差し込み
- 低確率の話題戻し・繰り返し
- モデル減衰（任意）

---

## 14. 受け入れ条件（チェックリスト）
- [ ] ログイン無しで動く
- [ ] モバイルで操作しやすい（片手）
- [ ] 会話ログが保存される
- [ ] 上限超過で古いログが消える
- [ ] ユーザー名・個人情報を保存しない
- [ ] 爺口調が一貫している
- [ ] 返答にランダムな“間”がある
- [ ] ときどき誤タイプする
- [ ] 返答が長文になりすぎない

---

## 15. 設計者回答（実装メモ）
### 15.1 学習対象 / seed
- 学習対象はユーザー発話のみ。
- seedは固定（60〜120行、合計2〜5KB）。相槌・曖昧・聞き返し・昔話導入・別れ・自己説明を含む。
- seedの自動更新はしない。開発者向けに「seed再投入」だけ用意してもよい（UIには出さない）。

### 15.2 bigram生成の停止条件
- 最大16トークン。
- 「。」「！」「？」が出たら、追撃トークンを1回だけ許可して停止。
- `<EOS>` が出たら即停止。
- 3文生成は低確率（例: 10%）でのみ許可。

### 15.3 生成の混合比
- v0.1〜v0.2は固定: template 55% / bigram 35% / fallback 10%。
- v0.3以降に会話回数でbigram比率を緩やかに上げる案は可（上限45%）。

### 15.4 キーワード抽出（最小ルール）
- tokenize後のトークンから選ぶ。
- 除外: 助詞っぽい短語（「は」「が」「を」「に」「で」「と」「も」「の」）、句読点、1文字。
- 優先: 2〜6文字、漢字/カタカナ優先。
- 候補なしなら差し込みなし。差し込みは0〜1個。

### 15.5 誤タイプ
- `typo.ts` / `typo.json` に独立。
- 1返答につき最大1箇所。
- 種別: かな置換 / 1文字欠落 / 「…」追加。
- typoRateは0.06（6%）。三点リーダ優先でOK。

### 15.6 export/import
- v0.1は不要。将来、開発者向けにJSONコピー程度なら可。

### 15.7 オフライン / PWA
- オフラインのみでOK。PWAは後回し。
- APIなし / fetch依存なし / 画像少なめで将来のPWA化に備える。

### 15.8 採用する改善提案
- 返信遅延を長さ比例に（短文0.6–1.2s、長文1.5–3.5s）。
- 感情タグはv0.2以降で採用（簡易ルール）。
- 英数字の連続は1トークンとして扱う。
- NG語は受け流しテンプレへ逃がす（最低限）。
- ループ防止: 同一トークン3連を禁止 / 直前と同一は重み半減。

### 15.9 実装前合意
- ブラウザ: iOS Safari最優先（次点: Chrome/Edge）。
- `maxChars` は60,000。
- UIはLINE風まで（バブル左右・入力固定・既読なし）。

---

設計日：2026年1月28日
追記日：2026年1月28日
設計者　月野テンプレクス
