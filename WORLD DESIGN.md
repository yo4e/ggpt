# GGPT 追加仕様：小さな和室の世界モデル（vNext）

## 目的（意図）
GGPTを「返答装置」から「そこに居る存在」へ寄せるため、**会話とは独立して進行する小さな世界（自室）**を導入する。
- 大きな知識・推論は不要
- **一貫した“場所”と“状態変化”**だけで、人間らしい「生活の割り込み」「独り言」「脱線」を作る
- 言語生成（ビグラム/トライグラム）を賢くするのではなく、**発話の理由（いま・ここ）**を付与する

## 世界（舞台）
**小さな和室**：ちゃぶ台／湯呑／窓／布団
- 世界は外界（ニュース等）を持たない
- 変化は室内の出来事と体感に限定

## 機能追加のゴール
1. 毎ターン（または一定間隔）で世界状態が少しだけ変化する
2. 世界状態から「ひとりごと（独り言）」が低確率で割り込む
3. 返答の一部が世界状態に影響される（語尾・話題種・短い挿入句）
4. 会話ログとは別に、世界状態は localStorage に保存され継続する

---

## データモデル
### state.world
```json
{
  "place": "washitsu",
  "time": {
    "phase": "morning|day|evening|night",
    "tick": 0,
    "lastUpdate": 0
  },
  "weather": {
    "type": "clear|rain|wind",
    "strength": 0
  },
  "room": {
    "windowOpen": false,
    "lightOn": false,
    "futonOut": true,
    "chabudaiClutter": 0
  },
  "items": {
    "yunomi": {
      "hasTea": true,
      "temp": "hot|warm|cold"
    }
  },
  "body": {
    "clarity": 0.15,
    "sleepiness": 0.6,
    "coldHands": 0.3,
    "backPain": 0.2
  },
  "memory": {
    "lastMonologue": "",
    "lastMonologueAt": 0
  }
}
```

### 保存先
- `localStorage['ggpt.world']`（または既存 state 保存に統合）
- 会話ログ削除とは独立（Reset方針で指定）

---

## 更新ルール（世界が“勝手に進む”）
### 更新タイミング
- `onUserMessage()` / `onAssistantReply()` のどちらかで **必ず1回** `worldStep()` を呼ぶ
- 加えて、一定時間経過（例：5分）で `worldStep()` を複数回進める（オフライン中の経過再現）

### worldStep() の基本
1. 時間帯 `phase` をゆっくり進める（tickで管理）
2. 天気を低頻度で遷移（clear↔rain↔wind、強度0〜2）
3. 室内フラグを確率で変化
   - `windowOpen`：夜は閉めやすい、暑い/換気で開けやすい（簡易でOK）
   - `lightOn`：夜は点きやすい
   - `futonOut`：夜はtrueになりやすい
4. 湯呑の状態
   - `hasTea` が true のとき時間で `temp` が hot→warm→cold
   - 低確率で「飲む」イベント：`hasTea=false`
   - 低確率で「淹れる」イベント：`hasTea=true,temp=hot`
5. 体感（body）
   - `clarity`：基本低め、たまにスパイク（覚醒）
   - `sleepiness`：夜に上がる
   - `coldHands`：雨/風/窓開で上がる
   - `backPain`：長時間同姿勢っぽいと上がる（tick依存でOK）

---

## 明晰度（clarity）の仕様（重要）
### ねらい
- 普段は相槌寄り（短文）
- 低確率で急に冴えて、少し長めに喋る
- “知能”ではなく“波”で生き物感を出す

### ルール
- `clarity` は 0.0〜1.0
- 通常：0.05〜0.25 に収束
- 低確率（例：3%）で **覚醒**：`clarity=0.75〜0.95` にジャンプ
- 覚醒は数ステップで減衰（例：`clarity *= 0.6`）

### 生成への影響
- clarity < 0.25：1〜6トークンで打ち切り／相槌・語尾中心
- 0.25〜0.65：通常短文（1〜2文）
- > 0.65：少し長め可（2〜4文）、話題を1回だけ展開可

---

## ひとりごと（Monologue）
### 発生条件
- 毎ステップ `p=0.08` 程度で発生（調整可能）
- ただし直近で発生したらクールダウン（例：30秒〜2分）

### 種（seed）の作り方：世界→話題
世界状態から優先順位で「種」を1つ選ぶ。
- 夜 & sleepiness高：眠い/布団
- rain：雨音/湿気
- wind：風/すきま風
- coldHands高：手が冷える/湯呑
- yunomi.temp=cold：茶が冷めた
- windowOpen=true：窓/風/音
- futonOut=true：布団/寝返り

### 出力の構成（テンプレ最小）
**テンプレ全文は避け、断片＋語尾だけ固定**。
- `prefix`（任意、短い）：例「……」「うむ」「そういえば」
- `seedLine`（短い核、世界由来）：例「茶が冷めたのう」
- `tail`（語尾フィルタ）：例「のう」「かの」「じゃ」

例：
- 「……雨の音がするのう」
- 「茶が冷めたかの」
- 「布団が呼んどるわい」

※ 独り言は原則ユーザーへの質問を含めない（ボット臭を減らす）

---

## 返答（Reply）への混ぜ方
### 方針
会話の主文はユーザー文から引っ張り、世界は**薄く混ぜる**。

### 混合ポイント（最小）
1. 返答の先頭に短い挿入句を入れる（低確率）
   - 例「湯呑が冷める前に…」「雨の音がするが…」
2. 返答の語尾を世界寄りに変える
   - 眠い→「…もう寝るかの」
   - coldHands高→「…手が冷えるのう」
3. clarityが高い時のみ、世界由来の比喩を1回許可

---

## トークナイズと学習（既存モデルを維持）
- 既存のビグラム/トライグラム学習は変更しない
- 世界モデルは **別レイヤ** として追加
- 独り言・挿入句は学習対象にしても良いが、初期は学習対象外でもOK（ノイズ混入を避ける）

---

## 実装タスク（最短）
1. `state.world` の追加と保存/読込
2. `worldStep()` 実装（時間・天気・湯呑・布団・体感・明晰度）
3. `maybeMonologue()` 実装（クールダウン付き）
4. `composeReply()` に「薄い混ぜ込み」フック追加
5. 設定（デバッグ用）
   - monologue頻度、覚醒率、クールダウン秒
   - world reset の範囲

---

## 期待する効果
- 返答が正しくなくても「生活がある」ことで許される
- 会話が噛み合わなくても「爺だから」で自然に見える
- まれに冴えることで「もしかして…？」が生まれる
- “世界のある独り言”が、存在感の核になる

---

設計者　月野テンプレクス

